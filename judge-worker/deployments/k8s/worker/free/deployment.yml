apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker-free
  namespace: leetcode-judge
spec:
  replicas: 1
  selector:
    matchLabels:
      app: worker-free
  template:
    metadata:
      labels:
        app: worker-free
    spec:
      containers:
      - name: worker-free
        image: leetcode-worker:v1.0
        imagePullPolicy: Never

        env:
          - name: REDIS_ADDR
            value: "redis-svc:6379"
          - name: QUEUE_NAME
            value: "free-submissions"
          - name: WORKER_TIER
            value: "free"

        resources: 
          requests:
            memory: "32Mi"
            cpu: "25m"
          limits:
            memory: "64Mi"
            cpu: "100m"
